global:
  name: consul
  datacenter: dc1

server:
  replicas: 3
  bootstrapExpect: 3
  storageClass: rustfs-sc
  disruptionBudget:
    enabled: true
    maxUnavailable: 1

connectInject:
  enabled: false

controller:
  enabled: false

ui:
  enabled: true

syncCatalog:
  enabled: false
